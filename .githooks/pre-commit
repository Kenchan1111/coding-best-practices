#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

exec python3 scripts/local_git_guard.py hook-check \
  --hook pre-commit \
  --refresh \
  --stage-manifests \
  --strict
