# skill/bin/

This directory is intentionally empty in Phase 1.

It is reserved for future runtime utilities (e.g. helper scripts, pre-commit
hooks, or host-specific executables) that may be added in Phase 2+.

Do not delete this directory — it is part of the skill package structure
expected by installation scripts.
